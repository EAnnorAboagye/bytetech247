User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: PerplexityBot
Allow: /

# Content Signals (contentsignals.org, draft-romm-aipref-contentsignals) were
# tried here as a crawler-agnostic layer on top of the per-bot Allow rules
# above, but Google added `content-signal` to its documented list of
# unsupported robots.txt directives in April 2026 (Search Off the Record),
# which Search Console's robots.txt report flags as an error rather than
# silently ignoring. Removed rather than left in place to keep that report
# clean. The per-bot Allow rules below already state the same search/AI
# stance (GPTBot, ClaudeBot, Google-Extended, PerplexityBot all explicitly
# allowed) without relying on a directive Google's parser rejects. Revisit
# if Content-Signal is ever added to Google's supported list.
User-agent: *
Allow: /

Sitemap: https://bytetech247.com/sitemap.xml
