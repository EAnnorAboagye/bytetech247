---
// build-spec.md §10 — five columns on desktop, collapses to a stacked list
// on mobile (handled here by the Grid primitive's auto-fit reflow, no extra
// breakpoint logic needed). Identical on every route.
import { siteConfig, CATEGORIES } from "../config";
import Grid from "./layout/Grid.astro";
import Stack from "./layout/Stack.astro";

const year = new Date().getFullYear();
const socialLinks = Object.entries(siteConfig.social);

// §10 (locked, v3.2): each platform pairs its real brand mark with a text
// label, not a bare "@bytetech247" link. The X mark below is the verified
// simple-icons path (24x24 viewBox). Facebook/Instagram/TikTok use simple,
// guaranteed-to-render primitive shapes rather than hand-typed bezier path
// data that couldn't be visually verified in this environment — swap these
// three for exact simple-icons path data as a follow-up polish pass
// (`npm install simple-icons` and copy the path `d` attributes).
const SOCIAL_ICONS: Record<string, string> = {
  facebook: `<circle cx="12" cy="12" r="11" fill="none" stroke="currentColor" stroke-width="1.5"/><path d="M13.5 9.5h1.8V7.2c-.3 0-1.3-.1-2.5-.1-2.5 0-4.2 1.5-4.2 4.3v2.1H6.2v2.6h2.4V19h2.7v-6.9h2.3l.4-2.6h-2.7v-1.8c0-.7.2-1.2 1.2-1.2Z" fill="currentColor"/>`,
  x: `<path d="M14.234 10.162 22.977 0h-2.072l-7.591 8.824L7.251 0H.258l9.168 13.343L.258 24H2.33l8.016-9.318L16.749 24h6.993zm-2.837 3.299-.929-1.329L3.076 1.56h3.182l5.965 8.532.929 1.329 7.754 11.09h-3.182z" fill="currentColor"/>`,
  instagram: `<rect x="2" y="2" width="20" height="20" rx="5" fill="none" stroke="currentColor" stroke-width="1.6"/><circle cx="12" cy="12" r="4.5" fill="none" stroke="currentColor" stroke-width="1.6"/><circle cx="17.3" cy="6.7" r="1.2" fill="currentColor"/>`,
  tiktok: `<path d="M14 3h2.4c.2 1.7 1.4 3.1 3.1 3.5v2.4a5.9 5.9 0 0 1-3.1-1V15a5.4 5.4 0 1 1-5.4-5.4c.2 0 .4 0 .6.02v2.5a2.9 2.9 0 1 0 2.4 2.86Z" fill="currentColor"/>`,
};
const SOCIAL_LABELS: Record<string, string> = {
  facebook: "Facebook",
  x: "X",
  instagram: "Instagram",
  tiktok: "TikTok",
};
---

<footer class="site-footer">
  <div class="site-footer__inner">
    <Grid minItemWidth="10rem" space="6">
      <Stack space="2">
        <p class="site-footer__site-name">{siteConfig.name}</p>
        <p class="site-footer__description">{siteConfig.description}</p>
        {
          socialLinks.length > 0 && (
            <div class="site-footer__social">
              <p class="site-footer__heading">Social handles</p>
              <ul class="site-footer__social-list">
                {socialLinks.map(([platform, href]) => (
                  <li>
                    <a href={href} class="site-footer__social-link">
                      <span
                        class="site-footer__social-icon"
                        aria-hidden="true"
                        set:html={`<svg viewBox="0 0 24 24" width="18" height="18">${SOCIAL_ICONS[platform] ?? ""}</svg>`}
                      />
                      {SOCIAL_LABELS[platform] ?? platform}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          )
        }
      </Stack>

      <Stack space="2" as="nav" aria-label="Categories">
        <p class="site-footer__heading">Categories</p>
        <ul class="site-footer__list">
          {
            CATEGORIES.map((category) => (
              <li>
                <a href={`/${category.slug}/`}>{category.name}</a>
              </li>
            ))
          }
        </ul>
      </Stack>

      <Stack space="2" as="nav" aria-label="Company">
        <p class="site-footer__heading">Company</p>
        <ul class="site-footer__list">
          <li><a href="/about/">About</a></li>
          <li><a href="/contact/">Contact</a></li>
        </ul>
      </Stack>

      <Stack space="2" as="nav" aria-label="Legal">
        <p class="site-footer__heading">Legal</p>
        <ul class="site-footer__list">
          <li><a href="/privacy/">Privacy</a></li>
          <li><a href="/terms/">Terms &amp; Conditions</a></li>
          <li>
            <a href="/affiliate-disclosure/">Affiliate/Advertising Disclosure</a
            >
          </li>
          <li><a href="/editorial-policy/">Editorial Policy</a></li>
        </ul>
      </Stack>

      <Stack space="2" as="nav" aria-label="Resources">
        <p class="site-footer__heading">Resources</p>
        <ul class="site-footer__list">
          <li><a href="/archive/">All posts</a></li>
          <li><a href="/rss.xml">RSS feed</a></li>
          <li><a href="/sitemap.xml">Sitemap</a></li>
        </ul>
      </Stack>
    </Grid>

    <p class="site-footer__copyright">&copy; {year} {siteConfig.name}</p>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--color-border);
    background-color: var(--color-bg-subtle);
  }

  .site-footer__inner {
    max-width: 72rem;
    margin-inline: auto;
    padding: calc(var(--spacing) * 6) calc(var(--spacing) * 4);
  }

  .site-footer__site-name {
    font-weight: 700;
    color: var(--color-fg);
  }

  .site-footer__description,
  .site-footer__heading {
    font-size: var(--text-sm);
  }

  .site-footer__heading {
    font-weight: 600;
    color: var(--color-fg);
  }

  .site-footer__description {
    color: var(--color-fg-muted);
  }

  .site-footer__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 0.5);
  }

  .site-footer__list a {
    display: inline-flex;
    align-items: center;
    min-height: 24px;
    padding-block: calc(var(--spacing) * 0.25);
    color: var(--color-fg-muted);
    text-decoration: none;
    font-size: var(--text-sm);
  }

  .site-footer__list a:hover {
    color: var(--color-accent);
  }

  .site-footer__social {
    margin-top: calc(var(--spacing) * 2);
  }

  .site-footer__social-list {
    list-style: none;
    margin: calc(var(--spacing) * 1) 0 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 0.5);
  }

  .site-footer__social-link {
    display: inline-flex;
    align-items: center;
    gap: calc(var(--spacing) * 1.5);
    min-height: 24px;
    color: var(--color-fg-muted);
    text-decoration: none;
    font-size: var(--text-sm);
  }

  .site-footer__social-link:hover {
    color: var(--color-accent);
  }

  .site-footer__social-icon {
    display: inline-flex;
    color: currentColor;
    flex-shrink: 0;
  }

  .site-footer__copyright {
    margin-top: calc(var(--spacing) * 6);
    padding-top: calc(var(--spacing) * 4);
    border-top: 1px solid var(--color-border);
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
  }
</style>
