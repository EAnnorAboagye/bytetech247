---
title: "Welcome to ByteTech247"
description: "A scaffold fixture proving category-based routing and the content collection pipeline work end to end."
date: 2026-07-26
category: dev-tools
tags: ["scaffold"]
coverImage: "./cover.jpg"
coverImageAlt: "A dark gradient placeholder cover image with the text ByteTech247"
---

This post is a scaffold fixture, not real content. It exists so `/[category]/[slug]` routing,
the full frontmatter schema, and the MDX components below can all be verified against a real
entry instead of an empty collection.

## A heading for the table of contents

Enough prose to give the reading-time calculator and table of contents something real to
measure, and to demonstrate that headings get picked up correctly.

### A GFM table

| Tool     | Language | Stars |
| -------- | -------- | ----- |
| Astro    | JS/TS    | 45k   |
| Vite     | JS/TS    | 65k   |
| Tailwind | CSS      | 80k   |

## Code blocks

Exercising Phase 4's code block engineering: a fixed dark theme, line highlighting, diff
notation, line numbers, a copy button, and a filename tab.

```js title="config.js" {2,4-5}
export function createConfig() {
  const base = { name: "ByteTech247" }; // highlighted

  return {
    ...base, // highlighted
    url: "https://bytetech247.workers.dev", // highlighted
  };
}
```

```js
function greet(name) {
  console.log("Hello " + name); // [!code --]
  console.log(`Hello ${name}`); // [!code ++]
}
```

<CodeTabs labels={["package.json", "index.js"]}>
  <Fragment slot="tab-0">
    ```json title="package.json"
    { "name": "bytetech247", "type": "module" }
    ```
  </Fragment>
  <Fragment slot="tab-1">
    ```js title="index.js"
    console.log("Hello, ByteTech247!");
    ```
  </Fragment>
</CodeTabs>

## Another heading

More filler content so the reading time isn't rounded down to zero minutes, and so the table
of contents has more than one entry to actually demonstrate scroll-spy behavior once it's
wired up.
